***************
*** 41,46 ****
  # which itelf invokes make, the warnings ensue.
  
! unset MAKEFLAGS
! 
  
  case $# in
--- 41,47 ----
  # which itelf invokes make, the warnings ensue.
  
! if [ "x$MAKEFLAGS" != "x" ]; then
!     unset MAKEFLAGS
! fi
  
  case $# in
