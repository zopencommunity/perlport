***************
*** 41,46 ****
  # which itelf invokes make, the warnings ensue.
  
! unset MAKEFLAGS
! 
  
  case $# in
--- 41,47 ----
  # which itelf invokes make, the warnings ensue.
  
! if ! [ -z ${MAKEFLAGS} ]; then
! 	unset MAKEFLAGS
! fi
  
  case $# in
